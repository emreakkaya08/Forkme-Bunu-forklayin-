/**
 * const [owner] = await ethers.getSigners();
    await contract.grantRole(
      ethers.utils.id("TOKEN_SETTER_ROLE"),
      owner.address
    );
    await contract.connect(owner).addTokenInfo();
 */
